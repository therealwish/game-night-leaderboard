<!DOCTYPE html>
<html>
<head>
  <title>Game Night Leaderboard</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>🏆 Game Night Leaderboard</h1>
  <h2>#USAPROCKS!!!</h2>
  <div class="leaderboard">
    <% teams.forEach((team, index) => { %>
      <div class="team <%= index == 0 ? 'first' : index == 1 ? 'second' : index == 2 ? 'third' : '' %>">b        
        <h2><a href="/team/<%= team[0] %>"><%= team[0] %></a></h2>
        <p><%= team[1].points %> points</p>
      </div>
    <% }); %>
  </div>
  
  <a href="/admin" class="admin-button">Admin Login</a>
  <footer>
    <p>Created by with love by wish <a href="www.wish.com">wish</a></p>
</body>
</html>
